import { Typography } from "@/components/ui";

export const Site = () => {
  return (
    <div>
      <Typography variant="h1" className="text-center">
        Site public page
      </Typography>
    </div>
  );
};
